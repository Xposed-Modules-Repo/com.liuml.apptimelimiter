# 酷安上架材料 - 时停

## 基础信息

- 应用名称：时停
- 英文名：Time Stop
- 包名：`com.liuml.apptimelimiter`
- 当前版本：`0.10.14`
- versionCode：`37`
- 分类建议：效率 / 工具 / 系统工具
- 关键词：屏幕时间、应用限制、时间管理、LSPosed、无障碍、Shizuku、专注、自律、应用管控

## 一句话简介

比系统屏幕时间更精细的应用使用边界：每日额度、单次打开、时段规则、分组共享和退出后冷却，可选 LSPosed 或非 Root 普通保护。

## 详细介绍

时停是一款面向设备所有者的 Android 应用时间管控工具。它不只是查看使用统计，而是为指定应用建立明确的使用规则：每天最多用多久、每次连续打开多久、哪些时段允许或禁止使用、到限后是否冷却，以及多个应用是否共享同一组额度。

主要能力：

1. 独立应用规则  
   每个应用可单独设置每日额度、单次打开限制、每周时段、提醒方式和冷却时长。

2. 应用分组  
   可把短视频、游戏、社交等应用放入同一组，共享每日额度、单次限制、时段和冷却。

3. 本次使用计划  
   打开受控应用时，可先选择本次预计使用时长。支持快捷选项和 1-1440 分钟数字输入，且不能超过当前剩余额度。

4. 多种保护模式  
   Root 用户可使用 LSPosed 模式获得更强执行效果；非 Root 用户可使用普通保护，通过无障碍前台包名事件和系统使用情况访问进行基础管控；可选 Shizuku 用于增强到限执行。

5. 到期提醒与限制页  
   支持顶部倒计时、全屏提醒、独立限制页和退出后冷却。限制页可跟随全局颜色主题和明暗模式。

6. 诊断与状态自检  
   内置保护状态面板和诊断日志，能查看 LSPosed 作用域、Hook 心跳、无障碍状态、使用情况访问、Shizuku 状态和限制触发过程。

隐私说明：

- 无障碍服务仅用于识别前台应用包名，不读取页面内容、文字、输入、账号或通知。
- 使用情况访问仅用于本机计算今日使用时长。
- 诊断数据保存在本机，不会自动上传。
- Shizuku 仅用于对已配置的第三方目标应用执行受限强停，不提供任意命令入口。

适用人群：

- 希望比系统屏幕时间设置更细规则的用户；
- 希望控制短视频、游戏、社交应用打开时长的用户；
- 需要 LSPosed / Shizuku / 非 Root 普通保护多种方案的 Android 玩机用户；
- 需要诊断日志确认规则是否真实生效的高级用户。

## 新版更新日志

- 新增全局保护模式：LSPosed、普通保护、普通保护 + Shizuku。
- 新增非 Root 普通保护，通过无障碍前台包名事件和系统使用情况访问进行基础管控。
- 新增可选 Shizuku 到限执行增强。
- 新增保护状态与修复面板。
- 新增本次计划统一输入组件、主题颜色和时间短句支持。
- 优化规则存储主库，避免 LSPosed 启停造成规则显示不一致。
- 优化桌面边界、限制页恢复、诊断日志和使用统计。

## 审核备注

本应用用于设备所有者本人进行自我时间管理。无障碍服务不读取窗口内容，`canRetrieveWindowContent=false`，仅根据系统提供的前台包名事件识别应用切换。应用不包含广告、不上传使用数据、不提供远程监控功能。

## 截图清单

建议准备 6-8 张截图：

1. 首页：展示当前保护状态和受控应用概览。
2. 应用规则页：展示每日额度、单次打开、时段、冷却。
3. 分组规则页：展示多个应用共享额度。
4. 保护模式设置：展示 LSPosed / 普通保护 / Shizuku。
5. 本次使用计划：展示快捷选项和分钟输入。
6. 独立限制页或倒计时提醒。
7. 诊断日志页。
8. 关于/隐私说明页。

